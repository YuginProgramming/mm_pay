/**
 * Контакт підтримки MULTIMASKING (email).
 * Використовуйте в усіх користувацьких текстах, де згадується підтримка.
 */
export const SUPPORT_EMAIL = "syla.krasy@gmail.com";

export const SUPPORT_MAILTO_URL = `mailto:${SUPPORT_EMAIL}`;

/** Закінчення повідомлення без HTML (копіюваний контакт). */
export const SUPPORT_CONTACT_SUFFIX_PLAIN_UA =
  `Підтримка: ${SUPPORT_EMAIL}`;
